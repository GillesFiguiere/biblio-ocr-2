export class Book {

    constructor(
        public title: string,
        public author: string,
        public publishDate: string,
        public isbn: string,
        public isLent: boolean
    ) {

    }
}